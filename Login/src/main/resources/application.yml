server:
  port: 9090

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/Denglu?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
    username: root
    password: root
  resources:
    static-locations: classpath:/templates  #访问静态页面

#mybatis:
  #mapper-locations: classpath:static/*Mapper.xml

#logging:
  #level:
    #com:
      #example:
        #mapper : debug